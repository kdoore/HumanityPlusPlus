<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Human Deep Architecture — Bridging Spiral</title>
<style>
@import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600&family=Crimson+Pro:ital,wght@0,300;0,400;1,300;1,400&family=Share+Tech+Mono&display=swap');

:root {
  --void: #05080f;
  --deep: #0a0f1a;
  --panel: #0f1520;
  --surface: #161e2e;
  --rim: #1e2d45;
  --spades:   #6ab4d4;  --spades-g: rgba(106,180,212,0.18);  --spades-d: rgba(106,180,212,0.06);
  --diamonds: #d4a84a;  --diamonds-g: rgba(212,168,74,0.18);  --diamonds-d: rgba(212,168,74,0.06);
  --hearts:   #d46878;  --hearts-g: rgba(212,104,120,0.18);   --hearts-d: rgba(212,104,120,0.06);
  --clubs:    #6dc498;  --clubs-g: rgba(109,196,152,0.18);    --clubs-d: rgba(109,196,152,0.06);
  --kindness: #c8b898;  --kindness-g: rgba(200,184,152,0.15);
  --imagine:  #fffbe0;  --imagine-g: rgba(255,251,224,0.12);
  --machine:  #7090b0;  --machine-g: rgba(112,144,176,0.15);
  --txt: #ddd8cc; --txt-mid: #8a8070; --txt-dim: #4a4540;
}

* { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }

body {
  background: var(--void);
  color: var(--txt);
  font-family: 'Crimson Pro', serif;
  min-height: 100vh;
  overflow-x: hidden;
}

/* ── LAYOUT ── */
.app { display: grid; grid-template-columns: 1fr 380px; min-height: 100vh; }
.main-canvas { position: relative; overflow: hidden; }
.side-panel {
  background: var(--deep);
  border-left: 1px solid var(--rim);
  display: flex; flex-direction: column;
  overflow-y: auto;
}

/* ── HEADER ── */
.header {
  position: absolute; top: 0; left: 0; right: 0;
  padding: 18px 28px 14px;
  border-bottom: 1px solid var(--rim);
  display: flex; align-items: center; justify-content: space-between;
  background: rgba(5,8,15,0.85);
  backdrop-filter: blur(8px);
  z-index: 20;
}
.header h1 {
  font-family: 'Cinzel', serif; font-weight: 400;
  font-size: 1rem; letter-spacing: 0.2em; color: var(--txt);
}
.header .sub {
  font-family: 'Share Tech Mono', monospace;
  font-size: 0.52rem; color: var(--txt-dim); letter-spacing: 0.2em; margin-top: 3px;
}

/* ── VIEW TABS ── */
.view-tabs {
  display: flex; gap: 6px; align-items: center;
}
.tab {
  padding: 5px 14px;
  font-family: 'Share Tech Mono', monospace;
  font-size: 0.58rem; letter-spacing: 0.15em;
  border: 1px solid var(--rim);
  background: transparent; color: var(--txt-dim);
  cursor: pointer; transition: all 0.2s;
}
.tab:hover { color: var(--txt); border-color: var(--kindness); }
.tab.active { background: var(--surface); color: var(--kindness); border-color: var(--kindness); }

/* ── DIAGRAM AREA ── */
.diagram-area {
  position: absolute; inset: 0; top: 56px;
  display: flex; align-items: center; justify-content: center;
  padding: 20px;
}

/* ── VIEWS ── */
.view { display: none; width: 100%; height: 100%; }
.view.active { display: flex; align-items: center; justify-content: center; }

/* ── STACK VIEW ── */
.stack-wrap {
  display: flex; flex-direction: column; align-items: center;
  gap: 0; width: 100%; max-width: 700px;
  position: relative;
}

.signal-column {
  position: absolute; left: 50%; transform: translateX(-50%);
  top: 0; bottom: 0; width: 2px;
  background: linear-gradient(180deg, var(--spades) 0%, var(--clubs) 100%);
  opacity: 0.12; pointer-events: none;
}

.layer-row {
  display: grid; grid-template-columns: 1fr 340px 1fr;
  align-items: center; width: 100%; gap: 0;
  position: relative;
}

.layer-connector {
  height: 28px; display: flex; justify-content: center; align-items: center;
  position: relative; z-index: 1;
}
.layer-connector .arr {
  font-size: 1.4rem; color: var(--txt-dim); opacity: 0.5;
  font-family: 'Share Tech Mono', monospace;
}
.layer-connector .arr-label {
  font-family: 'Share Tech Mono', monospace; font-size: 0.52rem;
  color: var(--txt-dim); letter-spacing: 0.15em; margin-left: 8px;
}

.layer-node {
  border: 1px solid var(--rim);
  border-left-width: 3px;
  padding: 14px 20px;
  cursor: pointer;
  transition: all 0.25s;
  position: relative;
  overflow: hidden;
  background: var(--panel);
  grid-column: 2;
}
.layer-node::before {
  content: ''; position: absolute; inset: 0; opacity: 0; transition: opacity 0.25s;
}
.layer-node:hover::before, .layer-node.expanded::before { opacity: 1; }

.layer-node.spades   { border-left-color: var(--spades);   } .layer-node.spades::before   { background: var(--spades-d); }
.layer-node.diamonds { border-left-color: var(--diamonds); } .layer-node.diamonds::before { background: var(--diamonds-d); }
.layer-node.hearts   { border-left-color: var(--hearts);   } .layer-node.hearts::before   { background: var(--hearts-d); }
.layer-node.clubs    { border-left-color: var(--clubs);    } .layer-node.clubs::before    { background: var(--clubs-d); }
.layer-node.kindness-node { border-left-color: var(--kindness); } .layer-node.kindness-node::before { background: var(--kindness-g); }
.layer-node.imagine-node  { border-left-color: var(--imagine); border-style: dashed; }
.layer-node.imagine-node::before  { background: var(--imagine-g); }
.layer-node.input-node { border-left-color: var(--machine); } .layer-node.input-node::before { background: var(--machine-g); }

.layer-header { display: flex; align-items: center; gap: 12px; position: relative; z-index: 1; }
.layer-suit { font-size: 1.6rem; line-height: 1; }
.layer-node.spades .layer-suit   { color: var(--spades); filter: drop-shadow(0 0 6px var(--spades)); }
.layer-node.diamonds .layer-suit { color: var(--diamonds); filter: drop-shadow(0 0 6px var(--diamonds)); }
.layer-node.hearts .layer-suit   { color: var(--hearts); filter: drop-shadow(0 0 6px var(--hearts)); }
.layer-node.clubs .layer-suit    { color: var(--clubs); filter: drop-shadow(0 0 6px var(--clubs)); }

.layer-title-group { flex: 1; }
.layer-name {
  font-family: 'Cinzel', serif; font-size: 0.85rem;
  letter-spacing: 0.12em; line-height: 1.2;
}
.layer-node.spades .layer-name   { color: var(--spades); }
.layer-node.diamonds .layer-name { color: var(--diamonds); }
.layer-node.hearts .layer-name   { color: var(--hearts); }
.layer-node.clubs .layer-name    { color: var(--clubs); }
.layer-node.kindness-node .layer-name { color: var(--kindness); }
.layer-node.imagine-node .layer-name  { color: var(--imagine); }
.layer-node.input-node .layer-name    { color: var(--machine); }

.layer-ml-tag {
  font-family: 'Share Tech Mono', monospace; font-size: 0.52rem;
  color: var(--txt-dim); letter-spacing: 0.15em; margin-top: 2px;
}
.layer-alch { font-size: 1rem; color: var(--txt-dim); opacity: 0.5; }
.layer-expand-icon {
  font-family: 'Share Tech Mono', monospace; font-size: 0.7rem;
  color: var(--txt-dim); transition: transform 0.25s;
}
.layer-node.expanded .layer-expand-icon { transform: rotate(90deg); }

.layer-body {
  max-height: 0; overflow: hidden;
  transition: max-height 0.4s cubic-bezier(0.4,0,0.2,1);
  position: relative; z-index: 1;
}
.layer-node.expanded .layer-body { max-height: 400px; }

.layer-body-inner { padding-top: 12px; }
.layer-desc {
  font-size: 0.9rem; line-height: 1.65; color: var(--txt-mid);
  font-style: italic; margin-bottom: 10px;
}
.layer-machine-parallel {
  padding: 8px 12px;
  border-left: 2px solid var(--machine);
  background: rgba(112,144,176,0.07);
  font-family: 'Share Tech Mono', monospace;
  font-size: 0.6rem; color: var(--machine); letter-spacing: 0.08em;
  line-height: 1.6; margin-bottom: 10px;
}
.subdial-chips { display: flex; flex-wrap: wrap; gap: 6px; }
.chip {
  padding: 3px 10px;
  font-family: 'Share Tech Mono', monospace; font-size: 0.55rem;
  border: 1px solid; letter-spacing: 0.1em;
}
.layer-node.spades .chip   { border-color: var(--spades); color: var(--spades); }
.layer-node.diamonds .chip { border-color: var(--diamonds); color: var(--diamonds); }
.layer-node.hearts .chip   { border-color: var(--hearts); color: var(--hearts); }
.layer-node.clubs .chip    { border-color: var(--clubs); color: var(--clubs); }

/* Side annotations */
.layer-side { display: flex; align-items: center; padding: 0 14px; }
.layer-side.left { justify-content: flex-end; }
.side-note {
  font-family: 'Share Tech Mono', monospace; font-size: 0.5rem;
  color: var(--txt-dim); letter-spacing: 0.12em; line-height: 1.8;
  text-align: right; opacity: 0;
  transition: opacity 0.3s;
}
.layer-node.expanded ~ .layer-side .side-note,
.layer-row:has(.layer-node.expanded) .side-note { opacity: 1; }
.layer-side.right .side-note { text-align: left; }

/* Flatten toggle */
.flatten-bar {
  margin-top: 16px;
  display: flex; align-items: center; gap: 12px; justify-content: center;
}
.flatten-btn {
  padding: 7px 18px;
  font-family: 'Share Tech Mono', monospace; font-size: 0.6rem;
  letter-spacing: 0.15em; border: 1px solid var(--hearts);
  background: transparent; color: var(--hearts); cursor: pointer;
  transition: all 0.2s;
}
.flatten-btn:hover { background: var(--hearts-g); }
.flatten-btn.active { background: rgba(212,104,120,0.2); }
.flatten-label {
  font-family: 'Share Tech Mono', monospace; font-size: 0.52rem;
  color: var(--txt-dim); letter-spacing: 0.12em;
}

/* Flattened state */
.stack-wrap.flattened .layer-node:not(.spades) { opacity: 0.18; }
.stack-wrap.flattened .layer-node.spades {
  border-left-color: var(--hearts);
  animation: flatten-pulse 1.5s ease-in-out infinite;
}
@keyframes flatten-pulse { 0%,100% { box-shadow: 0 0 0 0 rgba(212,104,120,0); } 50% { box-shadow: 0 0 20px 4px rgba(212,104,120,0.3); } }

/* ── MPCM VIEW ── */
.mpcm-wrap {
  width: 100%; max-width: 800px;
  position: relative;
}
.mpcm-river {
  position: relative; padding: 40px 0;
}
.mpcm-banks {
  position: absolute; left: 0; right: 0;
  height: 1px; background: linear-gradient(90deg, transparent, var(--rim), transparent);
}
.mpcm-banks.top { top: 0; }
.mpcm-banks.bottom { bottom: 0; }
.mpcm-nodes {
  display: flex; align-items: center; gap: 0;
  position: relative; z-index: 2;
}
.mpcm-node {
  flex: 1; text-align: center; padding: 24px 12px;
  cursor: pointer; transition: all 0.25s;
  border: 1px solid transparent; position: relative;
}
.mpcm-node:hover { background: rgba(255,255,255,0.03); }
.mpcm-node.active { border-color: var(--rim); background: var(--surface); }
.mpcm-letter {
  font-family: 'Cinzel', serif; font-weight: 600;
  font-size: 3.5rem; line-height: 1; margin-bottom: 6px;
}
.mpcm-node:nth-child(1) .mpcm-letter { color: var(--spades); text-shadow: 0 0 24px rgba(106,180,212,0.4); }
.mpcm-node:nth-child(3) .mpcm-letter { color: var(--diamonds); text-shadow: 0 0 24px rgba(212,168,74,0.4); }
.mpcm-node:nth-child(5) .mpcm-letter { color: var(--hearts); text-shadow: 0 0 24px rgba(212,104,120,0.4); }
.mpcm-node:nth-child(7) .mpcm-letter {
  color: var(--kindness); text-shadow: 0 0 24px rgba(200,184,152,0.5);
  font-style: italic;
}
.mpcm-name { font-family: 'Cinzel', serif; font-size: 0.7rem; letter-spacing: 0.2em; color: var(--txt-mid); }
.mpcm-sub { font-family: 'Share Tech Mono', monospace; font-size: 0.52rem; color: var(--txt-dim); margin-top: 4px; letter-spacing: 0.1em; }
.mpcm-domain { font-size: 0.8rem; font-style: italic; color: var(--txt-dim); margin-top: 8px; }
.mpcm-node:nth-child(7) .mpcm-domain { color: var(--kindness); }
.mpcm-node:nth-child(7) { border-style: dashed; }

.mpcm-arrow-wrap {
  display: flex; flex-direction: column; align-items: center;
  gap: 4px; padding: 0 6px;
}
.mpcm-arr { font-size: 1.2rem; color: var(--txt-dim); }
.mpcm-latent {
  font-family: 'Share Tech Mono', monospace; font-size: 0.45rem;
  color: var(--txt-dim); letter-spacing: 0.1em; text-align: center;
  line-height: 1.6; opacity: 0.6;
}
.latent-glow {
  width: 40px; height: 20px;
  background: radial-gradient(ellipse, rgba(255,251,224,0.15) 0%, transparent 70%);
  border-radius: 50%;
  animation: latent-breathe 3s ease-in-out infinite;
}
@keyframes latent-breathe {
  0%,100% { opacity: 0.4; transform: scaleX(1); }
  50% { opacity: 1; transform: scaleX(1.3); }
}

.mpcm-detail {
  background: var(--surface); border: 1px solid var(--rim);
  padding: 20px 24px; margin-top: 16px;
  min-height: 80px; transition: all 0.3s;
}
.mpcm-detail-title { font-family: 'Cinzel', serif; font-size: 0.9rem; letter-spacing: 0.12em; margin-bottom: 8px; }
.mpcm-detail-body { font-size: 0.9rem; line-height: 1.7; color: var(--txt-mid); font-style: italic; }
.mpcm-detail-machine {
  margin-top: 10px; padding: 8px 12px;
  border-left: 2px solid var(--machine);
  font-family: 'Share Tech Mono', monospace; font-size: 0.58rem;
  color: var(--machine); letter-spacing: 0.08em; line-height: 1.6;
  background: rgba(112,144,176,0.06);
}

/* ── DASHBOARD VIEW ── */
.dashboard-wrap {
  width: 100%; max-width: 640px;
  position: relative; display: flex;
  flex-direction: column; align-items: center;
}

.dial-svg-wrap { width: 100%; position: relative; }
.dial-svg-wrap svg { width: 100%; height: auto; }

/* SVG interaction hint */
.dial-hint {
  font-family: 'Share Tech Mono', monospace; font-size: 0.52rem;
  color: var(--txt-dim); letter-spacing: 0.15em; text-align: center; margin-top: 12px;
}

/* ── SIDE PANEL ── */
.side-header {
  padding: 18px 20px 14px;
  border-bottom: 1px solid var(--rim);
  font-family: 'Cinzel', serif; font-size: 0.8rem;
  letter-spacing: 0.15em; color: var(--txt-mid);
}

.side-content { padding: 20px; flex: 1; }

.detail-empty {
  font-style: italic; color: var(--txt-dim); font-size: 0.9rem;
  line-height: 1.7; text-align: center;
  padding: 40px 20px;
}
.detail-empty .prompt { font-size: 1.8rem; margin-bottom: 12px; color: var(--rim); }

.detail-block { display: none; }
.detail-block.visible { display: block; }

.detail-suit-row {
  display: flex; align-items: center; gap: 12px; margin-bottom: 14px;
}
.detail-suit { font-size: 2.4rem; line-height: 1; }
.detail-name { font-family: 'Cinzel', serif; font-size: 1.1rem; letter-spacing: 0.1em; }
.detail-tag { font-family: 'Share Tech Mono', monospace; font-size: 0.55rem; letter-spacing: 0.18em; color: var(--txt-dim); margin-top: 2px; }

.detail-section { margin-bottom: 14px; }
.detail-section-title {
  font-family: 'Share Tech Mono', monospace; font-size: 0.55rem;
  letter-spacing: 0.2em; color: var(--txt-dim); margin-bottom: 6px;
  padding-bottom: 4px; border-bottom: 1px solid var(--rim);
}
.detail-text { font-size: 0.88rem; line-height: 1.65; color: var(--txt-mid); font-style: italic; }
.detail-machine {
  padding: 8px 12px; border-left: 2px solid var(--machine);
  background: rgba(112,144,176,0.07);
  font-family: 'Share Tech Mono', monospace; font-size: 0.58rem;
  color: var(--machine); line-height: 1.6; letter-spacing: 0.06em;
}
.detail-chips { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 8px; }
.detail-chip {
  padding: 3px 9px;
  font-family: 'Share Tech Mono', monospace; font-size: 0.52rem;
  border: 1px solid; letter-spacing: 0.1em;
}

.aperture-meter {
  margin: 14px 0; padding: 12px;
  background: var(--panel); border: 1px solid var(--rim);
}
.aperture-label {
  font-family: 'Share Tech Mono', monospace; font-size: 0.55rem;
  letter-spacing: 0.2em; color: var(--txt-dim); margin-bottom: 8px;
}
.aperture-bar-wrap { display: flex; flex-direction: column; gap: 5px; }
.aperture-bar-row { display: flex; align-items: center; gap: 8px; }
.aperture-bar-name {
  font-family: 'Share Tech Mono', monospace; font-size: 0.52rem;
  color: var(--txt-dim); width: 28px;
}
.aperture-bar-track {
  flex: 1; height: 4px; background: var(--rim); border-radius: 2px; overflow: hidden;
}
.aperture-bar-fill { height: 100%; border-radius: 2px; transition: width 0.5s; }

/* Canvas for particles */
#particle-canvas {
  position: fixed; inset: 0; pointer-events: none; z-index: 0;
  opacity: 0.6;
}

/* Animations */
@keyframes fadeUp { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: translateY(0); } }
.stack-wrap, .mpcm-wrap, .dashboard-wrap { animation: fadeUp 0.5s ease both; }
</style>
</head>
<body>

<canvas id="particle-canvas"></canvas>

<div class="app">
  <!-- MAIN DIAGRAM AREA -->
  <div class="main-canvas">
    <div class="header">
      <div>
        <div class="header-h1" style="font-family:'Cinzel',serif;font-size:0.95rem;letter-spacing:0.2em;">BRIDGING SPIRAL — DEEP ARCHITECTURE</div>
        <div class="sub">HUMAN MEANING-MAKING AS NEURAL NETWORK · DASHBOARD DIALS SR2</div>
      </div>
      <div class="view-tabs">
        <button class="tab active" onclick="setView('stack')">DEEP STACK</button>
        <button class="tab" onclick="setView('mpcm')">MPCM RIVER</button>
        <button class="tab" onclick="setView('dashboard')">DASHBOARD</button>
      </div>
    </div>

    <div class="diagram-area">

      <!-- ── STACK VIEW ── -->
      <div class="view active" id="view-stack">
        <div class="stack-wrap" id="stackWrap">

          <!-- Input -->
          <div class="layer-row">
            <div class="layer-side left"></div>
            <div class="layer-node input-node" onclick="expandLayer(this, 'input')">
              <div class="layer-header">
                <div class="layer-suit">⬡</div>
                <div class="layer-title-group">
                  <div class="layer-name" style="color:var(--machine)">Input Layer — Raw Signal Arrives</div>
                  <div class="layer-ml-tag">ML: input vector · human: media / event / social stimulus</div>
                </div>
                <div class="layer-expand-icon">▶</div>
              </div>
              <div class="layer-body"><div class="layer-body-inner">
                <div class="layer-desc">The raw information arrives — media content, a social event, an AI-generated output, a person's words. At this stage it has not yet been interpreted or weighted. It is pure signal waiting to pass through the stack.</div>
                <div class="layer-machine-parallel">ML PARALLEL: input vector x — all features concatenated before any transformation. No weighting yet.</div>
              </div></div>
            </div>
            <div class="layer-side right"></div>
          </div>

          <div class="layer-connector"><span class="arr">↓</span><span class="arr-label">signal enters first layer</span></div>

          <!-- ♠ Somatic -->
          <div class="layer-row">
            <div class="layer-side left"><div class="side-note" style="color:var(--spades)">NONLINEARITY<br>the activation function<br>gates all other layers<br><br>🜃 Earth · North</div></div>
            <div class="layer-node spades" onclick="expandLayer(this, 'spades')">
              <div class="layer-header">
                <div class="layer-suit">♠</div>
                <div class="layer-title-group">
                  <div class="layer-name">Somatic Nonlinearity — Activation Layer</div>
                  <div class="layer-ml-tag">ML: pointwise nonlinearity (ReLU / sigmoid) · element-wise gating</div>
                </div>
                <div class="layer-alch">🜃</div>
                <div class="layer-expand-icon">▶</div>
              </div>
              <div class="layer-body"><div class="layer-body-inner">
                <div class="layer-desc">The body decides what gets amplified, suppressed, or transformed before anything reaches conscious interpretation. This is not preprocessing — it is the first deep layer. Without it, the entire stack collapses to a single linear transformation. This is the architectural argument for trauma-informed practice.</div>
                <div class="layer-machine-parallel">ML: without nonlinearity, stacking N linear layers = 1 linear layer. The nonlinearity IS the source of depth. Remove it and you have no architecture, only a mapping.</div>
                <div class="subdial-chips">
                  <span class="chip">♠ PI — Pause & Intention</span>
                  <span class="chip">♠ WT — Window of Tolerance</span>
                  <span class="chip">♠ FE — Felt Expectations</span>
                </div>
              </div></div>
            </div>
            <div class="layer-side right"><div class="side-note" style="color:var(--spades)">When this layer<br>is suppressed:<br>cognition flattens<br>brittle · reactive<br>single-narrative</div></div>
          </div>

          <div class="layer-connector"><span class="arr">↓</span><span class="arr-label">transformed signal passes to</span></div>

          <!-- ♦ Cognitive -->
          <div class="layer-row">
            <div class="layer-side left"><div class="side-note" style="color:var(--diamonds)">LINEAR LAYER<br>explicit reasoning<br>conscious modeling<br><br>🜁 Air · East</div></div>
            <div class="layer-node diamonds" onclick="expandLayer(this, 'diamonds')">
              <div class="layer-header">
                <div class="layer-suit">♦</div>
                <div class="layer-title-group">
                  <div class="layer-name">Cognitive Radar — Linear Transformation Layer</div>
                  <div class="layer-ml-tag">ML: weighted sum · W·x + b — deliberate recombination of inputs</div>
                </div>
                <div class="layer-alch">🜁</div>
                <div class="layer-expand-icon">▶</div>
              </div>
              <div class="layer-body"><div class="layer-body-inner">
                <div class="layer-desc">Conscious reasoning assembles weighted associations, scans frames and claims, holds multiple models simultaneously. This is the layer we most notice — and the one we most mistake for the whole system. Air discerns: the scanning mind that can hold more than one signal at once.</div>
                <div class="layer-machine-parallel">ML: linear transformation W·x — each neuron takes a weighted sum of all inputs. The weights encode learned associations. Powerful but limited — expressiveness comes from composition with nonlinearities.</div>
                <div class="subdial-chips">
                  <span class="chip">♦ FC — Frame/Claim Scan</span>
                  <span class="chip">♦ CC — Confidence T/I/F</span>
                  <span class="chip">♦ MM — Multi-Model</span>
                  <span class="chip">♦ IE — Incentives</span>
                </div>
              </div></div>
            </div>
            <div class="layer-side right"><div class="side-note" style="color:var(--diamonds)">The layer we<br>most notice<br>but never the<br>whole system</div></div>
          </div>

          <div class="layer-connector"><span class="arr">↓</span><span class="arr-label">output tested against</span></div>

          <!-- ♥ Hearts -->
          <div class="layer-row">
            <div class="layer-side left"><div class="side-note" style="color:var(--hearts)">LOSS FUNCTION<br>values define error<br>what counts as<br>getting it wrong<br><br>🜄 Water · South</div></div>
            <div class="layer-node hearts" onclick="expandLayer(this, 'hearts')">
              <div class="layer-header">
                <div class="layer-suit">♥</div>
                <div class="layer-title-group">
                  <div class="layer-name">Relational Compass — Loss Function</div>
                  <div class="layer-ml-tag">ML: loss function L(ŷ, y) — error signal that drives learning</div>
                </div>
                <div class="layer-alch">🜄</div>
                <div class="layer-expand-icon">▶</div>
              </div>
              <div class="layer-body"><div class="layer-body-inner">
                <div class="layer-desc">Does this output violate care, dignity, or non-harm? The relational compass is the loss function — it defines what counts as error. Without values as a loss function, no gradient signal is generated, and the system cannot learn from its mistakes. Verification is not only epistemic — it is relational. Trust forms through repair.</div>
                <div class="layer-machine-parallel">ML: the loss function is predefined by engineers. Human loss function carries moral and relational content — not just error magnitude. Values are not a soft add-on; they are the source of the learning gradient.</div>
                <div class="subdial-chips">
                  <span class="chip">♥ PV — Prosocial Values</span>
                  <span class="chip">♥ VS — Verification Scale</span>
                  <span class="chip">♥ CR — Consent & Repair</span>
                </div>
              </div></div>
            </div>
            <div class="layer-side right"><div class="side-note" style="color:var(--hearts)">Error signal<br>propagates back<br>⬆ gradient flows<br>upward to update<br>earlier layers</div></div>
          </div>

          <div class="layer-connector"><span class="arr">↓</span><span class="arr-label" style="color:var(--clubs)">error gradient propagates back · learning begins</span></div>

          <!-- ♣ Clubs -->
          <div class="layer-row">
            <div class="layer-side left"><div class="side-note" style="color:var(--clubs)">BACKPROP<br>weight update<br>across time<br>each cycle = new<br>dimension added<br><br>🜂 Fire · West</div></div>
            <div class="layer-node clubs" onclick="expandLayer(this, 'clubs')">
              <div class="layer-header">
                <div class="layer-suit">♣</div>
                <div class="layer-title-group">
                  <div class="layer-name">Dimensional Integration — Backpropagation</div>
                  <div class="layer-ml-tag">ML: backprop + gradient descent — weight update across the entire stack</div>
                </div>
                <div class="layer-alch">🜂</div>
                <div class="layer-expand-icon">▶</div>
              </div>
              <div class="layer-body"><div class="layer-body-inner">
                <div class="layer-desc">Each cycle of learning does not merely repeat — it adds a new axis. Understanding expands in dimensions. Fire transforms across time. Metacognitive capacity is developmental, built on prior experience. It cannot be shortcut, only cultivated. Sleep consolidation is the biological backpropagation pass.</div>
                <div class="layer-machine-parallel">ML: backpropagation propagates error gradient through all layers, updating every weight. In humans, this happens through reflection, analog practice, and sleep. Environments that demand immediate output skip the backprop pass — no weights update, no learning occurs.</div>
                <div class="subdial-chips">
                  <span class="chip">♣ RL — Recalibration Loop</span>
                  <span class="chip">♣ MU — Model Update</span>
                  <span class="chip">♣ Cn — Consolidation / Sleep</span>
                </div>
              </div></div>
            </div>
            <div class="layer-side right"><div class="side-note" style="color:var(--clubs)">Depth forms<br>over time<br>developmental<br>cannot be rushed<br>only cultivated</div></div>
          </div>

          <div class="layer-connector"><span class="arr" style="opacity:0.3">↓</span><span class="arr-label" style="color:var(--kindness)">emergent · not delivered</span></div>

          <!-- Imagination + Aperture + Kindness row -->
          <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;width:100%;max-width:680px;">
            <div class="layer-node imagine-node" onclick="expandLayer(this,'imagine')">
              <div class="layer-header">
                <div class="layer-suit" style="color:var(--imagine);filter:drop-shadow(0 0 8px rgba(255,251,224,0.5));animation:breathe 3s ease-in-out infinite">✦</div>
                <div class="layer-title-group">
                  <div class="layer-name" style="color:var(--imagine)">Imagination — Latent Space</div>
                  <div class="layer-ml-tag">ML: intermediate hidden representations · unrealised potential</div>
                </div>
                <div class="layer-expand-icon">▶</div>
              </div>
              <div class="layer-body"><div class="layer-body-inner">
                <div class="layer-desc" style="color:rgba(255,251,224,0.6)">Not a stage — the negative space between all stages. Lives in the gaps where meaning wants to form but has not yet crystallised. The richest information in the stack. Protecting imagination means protecting the right to remain uncertain long enough for something genuinely new to form.</div>
                <div class="layer-machine-parallel">ML latent space holds compressed patterns — powerful for generation but not alive to meaning. Human latent space holds unrealised possibility oriented toward significance. This is not mysticism — it is a structural difference in what the space is for.</div>
              </div></div>
            </div>
            <div class="layer-node kindness-node" onclick="expandLayer(this,'kindness')">
              <div class="layer-header">
                <div class="layer-suit" style="color:var(--kindness);filter:drop-shadow(0 0 8px rgba(200,184,152,0.5))">❄</div>
                <div class="layer-title-group">
                  <div class="layer-name">Kindness — Regularization</div>
                  <div class="layer-ml-tag">ML: L2 regularization · prevents overfitting to threat patterns</div>
                </div>
                <div class="layer-expand-icon">▶</div>
              </div>
              <div class="layer-body"><div class="layer-body-inner">
                <div class="layer-desc">The field condition that keeps the model adaptive. Without it, the system overfits to the most salient threats in its training data — seeing danger everywhere it has seen danger before, unable to generalise. Warm truth + firm limits + commitment to repair.</div>
                <div class="layer-machine-parallel">ML regularization adds a penalty for complexity to prevent overfitting. Kindness cannot be applied from outside without consent — it is an intrinsic field condition, not an external parameter. A — Aperture functions as the learning rate: internal, state-dependent, trauma-sensitive.</div>
              </div></div>
            </div>
          </div>

          <!-- Flatten toggle -->
          <div class="flatten-bar">
            <button class="flatten-btn" id="flattenBtn" onclick="toggleFlatten()">SUPPRESS SOMATIC LAYER</button>
            <span class="flatten-label">simulate architecture collapse</span>
          </div>
        </div>
      </div>

      <!-- ── MPCM VIEW ── -->
      <div class="view" id="view-mpcm">
        <div class="mpcm-wrap">
          <div style="font-family:'Share Tech Mono',monospace;font-size:0.55rem;color:var(--txt-dim);letter-spacing:0.2em;text-align:center;margin-bottom:16px;">CLICK ANY NODE TO EXPLORE — RIVER OF MEANING</div>
          <div class="mpcm-river">
            <div class="mpcm-banks top"></div>
            <div class="mpcm-banks bottom"></div>
            <div class="mpcm-nodes">
              <div class="mpcm-node" onclick="selectMPCM(this,'mat')">
                <div class="mpcm-letter">M</div>
                <div class="mpcm-name">MATERIAL</div>
                <div class="mpcm-sub">tokens · pixels · signals</div>
                <div class="mpcm-domain">engineering domain</div>
              </div>
              <div class="mpcm-arrow-wrap">
                <div class="mpcm-arr">→</div>
                <div class="latent-glow"></div>
                <div class="mpcm-latent">latent<br>space<br>✦</div>
              </div>
              <div class="mpcm-node" onclick="selectMPCM(this,'proc')">
                <div class="mpcm-letter">P</div>
                <div class="mpcm-name">PROCESS</div>
                <div class="mpcm-sub">ranking · routing · loops</div>
                <div class="mpcm-domain">engineering domain</div>
              </div>
              <div class="mpcm-arrow-wrap">
                <div class="mpcm-arr">→</div>
                <div class="latent-glow"></div>
                <div class="mpcm-latent">latent<br>space<br>✦</div>
              </div>
              <div class="mpcm-node" onclick="selectMPCM(this,'ctx')">
                <div class="mpcm-letter">C</div>
                <div class="mpcm-name">CONTEXT</div>
                <div class="mpcm-sub">body · incentives · relation</div>
                <div class="mpcm-domain">shared / contested</div>
              </div>
              <div class="mpcm-arrow-wrap">
                <div class="mpcm-arr" style="font-size:1.5rem;opacity:0.4">⇢</div>
                <div class="mpcm-latent">meaning<br>emerges<br>here</div>
              </div>
              <div class="mpcm-node" onclick="selectMPCM(this,'mean')" style="border-style:dashed;border-color:var(--rim);">
                <div class="mpcm-letter">M</div>
                <div class="mpcm-name">MEANING</div>
                <div class="mpcm-sub">emergent · not installed</div>
                <div class="mpcm-domain">✦ arts · education · imagination</div>
              </div>
            </div>
          </div>
          <div class="mpcm-detail" id="mpcmDetail">
            <div class="mpcm-detail-title" style="color:var(--txt-dim);font-family:'Share Tech Mono',monospace;font-size:0.6rem;letter-spacing:0.2em;">SELECT A NODE ABOVE</div>
            <div class="mpcm-detail-body">The river carries information from raw material through engineered process and contextual shaping toward emergent meaning. Imagination lives in the latent space between every node — the unrealised potential that cannot be routed.</div>
          </div>
          <div style="margin-top:16px;padding:12px 16px;border:1px solid rgba(200,184,152,0.2);background:rgba(200,184,152,0.04);">
            <div style="font-family:'Share Tech Mono',monospace;font-size:0.52rem;color:var(--kindness);letter-spacing:0.15em;margin-bottom:6px;">❄ BOUNDARY PRINCIPLE</div>
            <div style="font-size:0.85rem;color:var(--txt-mid);font-style:italic;line-height:1.6;">Engineers own M and P. Context crosses the boundary. Meaning is unreachable by engineering — it emerges in living minds at the intersection of all four. This is the argument for arts education in every technical curriculum.</div>
          </div>
        </div>
      </div>

      <!-- ── DASHBOARD VIEW ── -->
      <div class="view" id="view-dashboard">
        <div class="dashboard-wrap">
          <div style="font-family:'Share Tech Mono',monospace;font-size:0.55rem;color:var(--txt-dim);letter-spacing:0.2em;text-align:center;margin-bottom:16px;">CLICK ANY INSTRUMENT TO EXPLORE</div>
          <div class="dial-svg-wrap">
            <svg viewBox="0 0 560 520" id="dashSVG">
              <defs>
                <radialGradient id="coreGrad" cx="50%" cy="50%" r="50%">
                  <stop offset="0%" style="stop-color:rgba(255,251,224,0.25)"/>
                  <stop offset="60%" style="stop-color:rgba(255,251,224,0.06)"/>
                  <stop offset="100%" style="stop-color:transparent"/>
                </radialGradient>
                <radialGradient id="aperGrad" cx="50%" cy="50%" r="50%">
                  <stop offset="0%" style="stop-color:rgba(200,184,152,0.3)"/>
                  <stop offset="100%" style="stop-color:transparent"/>
                </radialGradient>
                <filter id="glow2">
                  <feGaussianBlur stdDeviation="3" result="cb"/>
                  <feMerge><feMergeNode in="cb"/><feMergeNode in="SourceGraphic"/></feMerge>
                </filter>
              </defs>

              <!-- Outer kindness field -->
              <circle cx="280" cy="260" r="250" fill="none" stroke="rgba(200,184,152,0.08)" stroke-width="1" stroke-dasharray="3,8"/>
              <text x="280" y="20" text-anchor="middle" fill="rgba(200,184,152,0.3)" font-size="9" font-family="'Share Tech Mono'">❄ KINDNESS FIELD — the container</text>
              <text x="280" y="510" text-anchor="middle" fill="rgba(200,184,152,0.3)" font-size="9" font-family="'Share Tech Mono'">❄ makes imagination safe to emerge</text>

              <!-- Aperture rings -->
              <circle cx="280" cy="260" r="190" fill="none" stroke="rgba(200,184,152,0.06)" stroke-width="1"/>
              <circle cx="280" cy="260" r="130" fill="none" stroke="rgba(200,184,152,0.06)" stroke-width="1"/>
              <circle cx="280" cy="260" r="75" fill="none" stroke="rgba(200,184,152,0.08)" stroke-width="1"/>

              <!-- Axis lines -->
              <line x1="280" y1="40" x2="280" y2="480" stroke="rgba(255,255,255,0.04)" stroke-width="1"/>
              <line x1="50" y1="260" x2="510" y2="260" stroke="rgba(255,255,255,0.04)" stroke-width="1"/>
              <line x1="100" y1="110" x2="460" y2="410" stroke="rgba(255,255,255,0.02)" stroke-width="1" stroke-dasharray="2,10"/>
              <line x1="460" y1="110" x2="100" y2="410" stroke="rgba(255,255,255,0.02)" stroke-width="1" stroke-dasharray="2,10"/>

              <!-- Center imagination glow -->
              <circle cx="280" cy="260" r="72" fill="url(#coreGrad)"/>
              <text x="280" y="255" text-anchor="middle" fill="rgba(255,251,224,0.7)" font-size="20" font-family="'Palatino Linotype'">✦</text>
              <text x="280" y="272" text-anchor="middle" fill="rgba(255,251,224,0.45)" font-size="9" font-family="'Share Tech Mono'" letter-spacing="2">IMAGINATION</text>
              <text x="280" y="285" text-anchor="middle" fill="rgba(255,251,224,0.25)" font-size="7" font-family="'Share Tech Mono'">latent space</text>

              <!-- Aperture A label -->
              <circle cx="280" cy="260" r="74" fill="none" stroke="rgba(200,184,152,0.25)" stroke-width="1.5"/>
              <text x="280" y="222" text-anchor="middle" fill="rgba(200,184,152,0.6)" font-size="11" font-family="'Cinzel'" letter-spacing="3">A</text>
              <text x="280" y="234" text-anchor="middle" fill="rgba(200,184,152,0.3)" font-size="7" font-family="'Share Tech Mono'" letter-spacing="2">APERTURE</text>

              <!-- ♠ NORTH — Somatic Gyroscope -->
              <g class="svg-instrument" id="svgSpades" onclick="selectDash('spades')" style="cursor:pointer">
                <circle cx="280" cy="110" r="60" fill="rgba(106,180,212,0.08)" stroke="rgba(106,180,212,0.2)" stroke-width="1.5"/>
                <!-- Gyroscope rings -->
                <ellipse cx="280" cy="110" rx="48" ry="16" fill="none" stroke="rgba(106,180,212,0.35)" stroke-width="1.2"
                  style="animation:gyroSpin 8s linear infinite;transform-origin:280px 110px"/>
                <ellipse cx="280" cy="110" rx="16" ry="48" fill="none" stroke="rgba(106,180,212,0.25)" stroke-width="1.2"
                  style="animation:gyroSpin 12s linear infinite reverse;transform-origin:280px 110px"/>
                <circle cx="280" cy="110" r="10" fill="none" stroke="rgba(106,180,212,0.7)" stroke-width="1.5"/>
                <circle cx="280" cy="110" r="3" fill="rgba(106,180,212,0.9)" filter="url(#glow2)"/>
                <text x="280" y="175" text-anchor="middle" fill="rgba(106,180,212,0.9)" font-size="14" font-family="'Palatino Linotype'" filter="url(#glow2)">♠</text>
                <text x="280" y="190" text-anchor="middle" fill="rgba(106,180,212,0.7)" font-size="9" font-family="'Cinzel'" letter-spacing="2">SOMATIC</text>
                <text x="280" y="202" text-anchor="middle" fill="rgba(106,180,212,0.4)" font-size="7" font-family="'Share Tech Mono'">GYROSCOPE · 🜃 EARTH</text>
              </g>

              <!-- ♦ EAST — Cognitive Radar -->
              <g class="svg-instrument" id="svgDiamonds" onclick="selectDash('diamonds')" style="cursor:pointer">
                <circle cx="420" cy="260" r="60" fill="rgba(212,168,74,0.08)" stroke="rgba(212,168,74,0.2)" stroke-width="1.5"/>
                <!-- Radar rings -->
                <circle cx="420" cy="260" r="48" fill="none" stroke="rgba(212,168,74,0.12)" stroke-width="1"/>
                <circle cx="420" cy="260" r="32" fill="none" stroke="rgba(212,168,74,0.12)" stroke-width="1"/>
                <line x1="420" y1="215" x2="420" y2="308" stroke="rgba(212,168,74,0.1)" stroke-width="1"/>
                <line x1="374" y1="260" x2="466" y2="260" stroke="rgba(212,168,74,0.1)" stroke-width="1"/>
                <!-- Sweep -->
                <g style="animation:radarSweep 3s linear infinite;transform-origin:420px 260px">
                  <line x1="420" y1="260" x2="420" y2="214" stroke="rgba(212,168,74,0.8)" stroke-width="1.5"/>
                  <path d="M420,260 L420,214 A46,46 0,0,1 452,294 Z" fill="rgba(212,168,74,0.06)"/>
                </g>
                <circle cx="435" cy="242" r="2.5" fill="rgba(212,168,74,0.8)" style="animation:pulse 2s infinite"/>
                <circle cx="405" cy="278" r="1.5" fill="rgba(212,168,74,0.5)" style="animation:pulse 3s infinite 0.7s"/>
                <circle cx="420" cy="260" r="3" fill="rgba(212,168,74,0.9)" filter="url(#glow2)"/>
                <text x="352" y="265" text-anchor="middle" fill="rgba(212,168,74,0.9)" font-size="14" font-family="'Palatino Linotype'" filter="url(#glow2)">♦</text>
                <text x="352" y="280" text-anchor="middle" fill="rgba(212,168,74,0.7)" font-size="9" font-family="'Cinzel'" letter-spacing="2">COGNITIVE</text>
                <text x="352" y="292" text-anchor="middle" fill="rgba(212,168,74,0.4)" font-size="7" font-family="'Share Tech Mono'">RADAR · 🜁 AIR</text>
              </g>

              <!-- ♥ SOUTH — Relational Compass -->
              <g class="svg-instrument" id="svgHearts" onclick="selectDash('hearts')" style="cursor:pointer">
                <circle cx="280" cy="410" r="60" fill="rgba(212,104,120,0.08)" stroke="rgba(212,104,120,0.2)" stroke-width="1.5"/>
                <!-- Compass rose -->
                <polygon points="280,364 274,390 280,382 286,390" fill="rgba(212,104,120,0.85)"/>
                <polygon points="280,456 274,430 280,438 286,430" fill="rgba(212,104,120,0.25)"/>
                <polygon points="233,410 260,404 252,410 260,416" fill="rgba(212,104,120,0.25)"/>
                <polygon points="327,410 300,404 308,410 300,416" fill="rgba(212,104,120,0.25)"/>
                <circle cx="280" cy="410" r="8" fill="none" stroke="rgba(212,104,120,0.6)" stroke-width="1.5"/>
                <circle cx="280" cy="410" r="3" fill="rgba(212,104,120,0.9)" filter="url(#glow2)"/>
                <text x="280" y="340" text-anchor="middle" fill="rgba(212,104,120,0.9)" font-size="14" font-family="'Palatino Linotype'" filter="url(#glow2)">♥</text>
                <text x="280" y="355" text-anchor="middle" fill="rgba(212,104,120,0.7)" font-size="9" font-family="'Cinzel'" letter-spacing="2">RELATIONAL</text>
                <text x="280" y="367" text-anchor="middle" fill="rgba(212,104,120,0.4)" font-size="7" font-family="'Share Tech Mono'">COMPASS · 🜄 WATER</text>
              </g>

              <!-- ♣ WEST — Dimensional Integration -->
              <g class="svg-instrument" id="svgClubs" onclick="selectDash('clubs')" style="cursor:pointer">
                <circle cx="140" cy="260" r="60" fill="rgba(109,196,152,0.08)" stroke="rgba(109,196,152,0.2)" stroke-width="1.5"/>
                <!-- Expanding sphere -->
                <ellipse cx="140" cy="260" rx="48" ry="20" fill="none" stroke="rgba(109,196,152,0.25)" stroke-width="1.2"
                  style="animation:gyroSpin 14s linear infinite;transform-origin:140px 260px"/>
                <ellipse cx="140" cy="260" rx="20" ry="48" fill="none" stroke="rgba(109,196,152,0.2)" stroke-width="1.2"
                  style="animation:gyroSpin 18s linear infinite reverse;transform-origin:140px 260px"/>
                <path d="M140,260 Q156,244 152,260 Q148,276 140,272 Q124,268 126,260 Q128,244 140,242"
                  fill="none" stroke="rgba(109,196,152,0.6)" stroke-width="1.5"/>
                <circle cx="140" cy="260" r="3" fill="rgba(109,196,152,0.9)" filter="url(#glow2)"/>
                <text x="208" y="265" text-anchor="middle" fill="rgba(109,196,152,0.9)" font-size="14" font-family="'Palatino Linotype'" filter="url(#glow2)">♣</text>
                <text x="208" y="280" text-anchor="middle" fill="rgba(109,196,152,0.7)" font-size="9" font-family="'Cinzel'" letter-spacing="2">DIMENSIONAL</text>
                <text x="208" y="292" text-anchor="middle" fill="rgba(109,196,152,0.4)" font-size="7" font-family="'Share Tech Mono'">INTEGRATION · 🜂 FIRE</text>
              </g>

            </svg>
          </div>
          <div class="dial-hint">CLICK INSTRUMENT · APERTURE AT CENTER · KINDNESS IS THE FIELD</div>
        </div>
      </div>

    </div>
  </div>

  <!-- SIDE PANEL -->
  <div class="side-panel">
    <div class="side-header">INSTRUMENT DETAIL</div>
    <div class="side-content" id="sideContent">
      <div class="detail-empty">
        <div class="prompt">◎</div>
        Click any layer, node, or instrument in the diagram to explore its architecture, function, and machine learning parallel.
      </div>
      <!-- Detail blocks injected by JS -->
    </div>
  </div>
</div>

<style>
@keyframes gyroSpin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
@keyframes radarSweep { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
@keyframes pulse { 0%,100%{opacity:0.4}50%{opacity:1} }
@keyframes breathe { 0%,100%{filter:drop-shadow(0 0 10px rgba(255,251,224,0.3));opacity:0.8}50%{filter:drop-shadow(0 0 22px rgba(255,251,224,0.7));opacity:1} }
</style>

<script>
// ── DATA ──────────────────────────────────────────────────────────
const DETAILS = {
  input: {
    suit: '⬡', name: 'Input Layer', tag: 'RAW SIGNAL · MEDIA · EVENT · STIMULUS',
    color: 'var(--machine)',
    desc: 'The raw information arrives — media content, a social event, an AI-generated output, a person\'s words. At this stage it has not yet been interpreted, weighted, or transformed.',
    machine: 'ML INPUT LAYER: the feature vector x containing all raw inputs before any transformation. No weighting, no filtering — pure signal.',
    chips: [],
    aperture: null,
  },
  spades: {
    suit: '♠', name: 'Somatic Gyroscope', tag: 'ACTIVATION LAYER · NONLINEARITY · 🜃 EARTH',
    color: 'var(--spades)',
    desc: 'The body decides what gets amplified, suppressed, or transformed before anything reaches conscious interpretation. This is the first deep layer — and without it, the entire cognitive stack collapses into a single linear transformation. Somatics provide grounding: like gravity, like the body\'s weight against the floor. The gyroscope senses orientation in turbulence.',
    machine: 'ML: pointwise nonlinearity (ReLU/sigmoid). Remove this and stacking N linear layers = 1 linear layer — no depth, no expressiveness. The nonlinearity IS the source of depth. This is the architectural argument for trauma-informed practice.',
    chips: ['♠ PI — Pause & Intention', '♠ WT — Window of Tolerance', '♠ FE — Felt Expectations'],
    aperture: { label: 'Aₛ — Somatic bandwidth', q: 'Can I sense and settle?', pct: 85 },
  },
  diamonds: {
    suit: '♦', name: 'Cognitive Radar', tag: 'LINEAR LAYER · DISCERNMENT · 🜁 AIR',
    color: 'var(--diamonds)',
    desc: 'Conscious reasoning assembles weighted associations, scans frames and claims, holds multiple models simultaneously. This is the layer we most notice — and the one we most mistake for the whole system. Air discerns: the scanning mind that can hold more than one signal at once. Premature convergence is the enemy of learning.',
    machine: 'ML: linear transformation W·x + b. Each unit computes a weighted sum of all inputs. Powerful but expressive only in composition with nonlinearities — which is why the somatic layer must come first.',
    chips: ['♦ FC — Frame/Claim Scan', '♦ CC — Confidence T/I/F', '♦ MM — Multi-Model Compare', '♦ IE — Incentives & Externalities'],
    aperture: { label: 'A꜀ — Cognitive bandwidth', q: 'Can I hold more than one model?', pct: 70 },
  },
  hearts: {
    suit: '♥', name: 'Relational Compass', tag: 'LOSS FUNCTION · VALUES · 🜄 WATER',
    color: 'var(--hearts)',
    desc: 'Does this output violate care, dignity, or non-harm? The relational compass is the loss function — it defines what counts as error. Without values as a loss function, no gradient signal is generated, and the system cannot learn from its mistakes. Water flows and connects. Trust forms through repair, not through accuracy alone.',
    machine: 'ML: loss function L(ŷ, y). Predefined by engineers in machines. In humans, carries moral and relational content — not just error magnitude. Values are not soft — they are the gradient source. No values = no learning signal.',
    chips: ['♥ PV — Prosocial Values Field', '♥ VS — Verification Scale', '♥ CR — Consent & Repair'],
    aperture: { label: 'Aᵣ — Relational bandwidth', q: 'Can I include other minds without threat?', pct: 60 },
  },
  clubs: {
    suit: '♣', name: 'Dimensional Integration', tag: 'BACKPROPAGATION · TIME · 🜂 FIRE',
    color: 'var(--clubs)',
    desc: 'Each cycle of learning adds a new axis. Understanding expands in dimensions. Fire transforms across time. Metacognitive capacity is developmental — it cannot be shortcut, only cultivated through analog practice, sleep consolidation, and lived iteration. Sleep is biological backpropagation.',
    machine: 'ML: backprop propagates error gradient through all layers, updating every weight. Environments that demand immediate output skip the backprop pass. No reflection = no weight update = no learning. Depth is not speed.',
    chips: ['♣ RL — Recalibration Loop', '♣ MU — Model Update', '♣ Cn — Consolidation / Sleep'],
    aperture: null,
  },
  imagine: {
    suit: '✦', name: 'Imagination', tag: 'LATENT SPACE · UNREALISED POTENTIAL',
    color: 'var(--imagine)',
    desc: 'Not a stage — the negative space between all stages. Lives in the gaps where meaning wants to form but has not yet crystallised. The richest information in the stack. Protecting imagination means protecting the right to remain uncertain long enough for something genuinely new to form. This is the function of arts education.',
    machine: 'ML latent space holds compressed statistical patterns — powerful for generation, not alive to meaning. Human imagination holds unrealised possibility oriented toward significance. This is not mysticism — it is a structural difference in what the space is for.',
    chips: [],
    aperture: null,
  },
  kindness: {
    suit: '❄', name: 'Kindness Resonance', tag: 'REGULARIZATION · FIELD CONDITION',
    color: 'var(--kindness)',
    desc: 'Warm truth + firm limits + commitment to repair. The field condition that prevents the model from overfitting to threat. Without kindness, the cognitive system overfits to the most salient dangers in its training data — seeing threat everywhere it has seen threat before, unable to generalise. Aperture functions as the learning rate: internal, state-dependent, trauma-sensitive.',
    machine: 'ML: L2 regularization adds penalty for over-reliance on any single pattern. Kindness is intrinsic — it cannot be applied from outside without consent. A destabilised kindness field reduces learning rate toward zero.',
    chips: ['A — Aperture: learning rate dial', 'Internal · State-dependent · Trauma-sensitive'],
    aperture: null,
  },
};

const MPCM_DATA = {
  mat: {
    title: 'M — Material', color: 'var(--spades)',
    body: 'What is this made of? Text, image, voice, feed, AI summary, interface. In AI systems, material is often pre-processed and hidden — users receive outputs without access to the source material that shaped them.',
    machine: 'ML INPUT: the raw feature vector. Engineers design what counts as material and how it is encoded. This shaping happens before any user interaction.',
  },
  proc: {
    title: 'P — Process', color: 'var(--diamonds)',
    body: 'What generated or amplified it? Training and ranking algorithms, persuasion loops, editing, automation, virality. This is where AI systems operate — routing, filtering, amplifying. Process is the site of immense power.',
    machine: 'ML ARCHITECTURE: the model weights and architecture that transform material into output. Engineers own this domain. Power over Process is not the same as power over Meaning.',
  },
  ctx: {
    title: 'C — Context', color: 'var(--hearts)',
    body: 'What surrounds it? Incentives, audience, timing, your nervous system state, cultural pressure. Context is relational — it includes you. Without context, claims float free of accountability. Context crosses the engineering boundary.',
    machine: 'ML DEPLOYMENT CONTEXT: the conditions under which the model is used — not fully controllable by engineers. Context is where the human enters the loop. It is the contested zone between engineering and meaning.',
  },
  mean: {
    title: 'M — Meaning (emergent)', color: 'var(--kindness)',
    body: 'What is forming in me? Emotion, assumption, normalisation, identity posture. Meaning is not delivered — it emerges in living minds at the intersection of all four layers. This is the domain of arts education, interdisciplinary inquiry, and human imagination. It cannot be automated.',
    machine: 'ML HAS NO EQUIVALENT: outputs are statistically plausible completions, not meaningful claims. Meaning requires a living mind with somatic nonlinearity, relational values, and temporal depth. This is why AI cannot replace the teacher.',
  },
};

const DASH_DATA = {
  spades: DETAILS.spades,
  diamonds: DETAILS.diamonds,
  hearts: DETAILS.hearts,
  clubs: DETAILS.clubs,
};

// ── VIEW SWITCHING ────────────────────────────────────────────────
function setView(v) {
  document.querySelectorAll('.view').forEach(el => el.classList.remove('active'));
  document.querySelectorAll('.tab').forEach(el => el.classList.remove('active'));
  document.getElementById('view-' + v).classList.add('active');
  document.querySelectorAll('.tab')[['stack','mpcm','dashboard'].indexOf(v)].classList.add('active');
  clearSide();
}

// ── LAYER EXPAND ─────────────────────────────────────────────────
function expandLayer(node, key) {
  const wasExpanded = node.classList.contains('expanded');
  document.querySelectorAll('.layer-node').forEach(n => n.classList.remove('expanded'));
  if (!wasExpanded) {
    node.classList.add('expanded');
    showSide(key);
  } else {
    clearSide();
  }
}

// ── MPCM SELECT ──────────────────────────────────────────────────
function selectMPCM(node, key) {
  document.querySelectorAll('.mpcm-node').forEach(n => n.classList.remove('active'));
  node.classList.add('active');
  const d = MPCM_DATA[key];
  const el = document.getElementById('mpcmDetail');
  el.innerHTML = `
    <div class="mpcm-detail-title" style="color:${d.color};font-family:'Cinzel',serif;font-size:0.9rem;letter-spacing:0.12em;">${d.title}</div>
    <div class="mpcm-detail-body">${d.body}</div>
    <div class="mpcm-detail-machine">${d.machine}</div>
  `;
  showSide(key === 'mat' ? 'spades' : key === 'proc' ? 'diamonds' : key === 'ctx' ? 'hearts' : 'imagine');
}

// ── DASHBOARD SELECT ─────────────────────────────────────────────
function selectDash(key) {
  showSide(key);
}

// ── SIDE PANEL ───────────────────────────────────────────────────
function showSide(key) {
  const d = DETAILS[key];
  if (!d) return;
  const chipHTML = d.chips.map(c => `<span class="detail-chip" style="border-color:${d.color};color:${d.color}">${c}</span>`).join('');
  const apertureHTML = d.aperture ? `
    <div class="aperture-meter">
      <div class="aperture-label">${d.aperture.label}</div>
      <div style="font-size:0.82rem;font-style:italic;color:var(--txt-mid);margin-bottom:8px;">${d.aperture.q}</div>
      <div class="aperture-bar-wrap">
        <div class="aperture-bar-row">
          <div class="aperture-bar-name" style="color:${d.color}">Aₛ</div>
          <div class="aperture-bar-track"><div class="aperture-bar-fill" style="background:${d.color};width:${d.aperture.pct}%"></div></div>
        </div>
      </div>
    </div>
  ` : '';
  document.getElementById('sideContent').innerHTML = `
    <div class="detail-block visible">
      <div class="detail-suit-row">
        <div class="detail-suit" style="color:${d.color};filter:drop-shadow(0 0 8px ${d.color})">${d.suit}</div>
        <div>
          <div class="detail-name" style="color:${d.color}">${d.name}</div>
          <div class="detail-tag">${d.tag}</div>
        </div>
      </div>
      <div class="detail-section">
        <div class="detail-section-title">FUNCTION</div>
        <div class="detail-text">${d.desc}</div>
      </div>
      ${apertureHTML}
      <div class="detail-section">
        <div class="detail-section-title">MACHINE LEARNING PARALLEL</div>
        <div class="detail-machine">${d.machine}</div>
      </div>
      ${d.chips.length ? `<div class="detail-section"><div class="detail-section-title">SUB-DIALS</div><div class="detail-chips">${chipHTML}</div></div>` : ''}
    </div>
  `;
}

function clearSide() {
  document.getElementById('sideContent').innerHTML = `
    <div class="detail-empty">
      <div class="prompt">◎</div>
      Click any layer, node, or instrument to explore its architecture, function, and machine learning parallel.
    </div>
  `;
}

// ── FLATTEN TOGGLE ────────────────────────────────────────────────
function toggleFlatten() {
  const wrap = document.getElementById('stackWrap');
  const btn = document.getElementById('flattenBtn');
  const isFlat = wrap.classList.contains('flattened');
  wrap.classList.toggle('flattened');
  btn.classList.toggle('active');
  btn.textContent = isFlat ? 'SUPPRESS SOMATIC LAYER' : 'RESTORE ARCHITECTURE';
  if (!isFlat) {
    showSide('hearts'); // show the consequence — loss of depth
    document.getElementById('sideContent').innerHTML = `
      <div class="detail-block visible">
        <div class="detail-suit-row">
          <div class="detail-suit" style="color:var(--hearts);filter:drop-shadow(0 0 8px var(--hearts))">⚠</div>
          <div>
            <div class="detail-name" style="color:var(--hearts)">Architecture Collapsed</div>
            <div class="detail-tag">SOMATIC LAYER SUPPRESSED</div>
          </div>
        </div>
        <div class="detail-section">
          <div class="detail-section-title">WHAT HAPPENS</div>
          <div class="detail-text">Without the somatic nonlinearity, all stacked layers collapse into a single linear transformation. The system still processes information — but it has no depth. It produces outputs without genuine understanding. This is what happens when educational environments, workplaces, or media ecosystems demand that humans suppress emotion to be taken seriously.</div>
        </div>
        <div class="detail-section">
          <div class="detail-section-title">ML EQUIVALENT</div>
          <div class="detail-machine">Remove all activation functions from a deep network → the entire multi-layer stack becomes mathematically equivalent to one linear layer. You cannot recover depth by adding more linear layers. The nonlinearity must be present at each level.</div>
        </div>
        <div class="detail-section">
          <div class="detail-section-title">RESULT IN HUMANS</div>
          <div class="detail-text" style="color:var(--hearts)">Brittle, reactive, single-narrative cognition. Tunnel vision. Urgency and contempt for uncertainty. Story lock-in. Loss of imagination. Inability to learn from error. These are not character flaws — they are the architectural consequences of a suppressed activation function.</div>
        </div>
      </div>
    `;
  } else {
    clearSide();
  }
}

// ── PARTICLE CANVAS ───────────────────────────────────────────────
const canvas = document.getElementById('particle-canvas');
const ctx2 = canvas.getContext('2d');
let particles = [];

function resizeCanvas() {
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
}
window.addEventListener('resize', resizeCanvas);
resizeCanvas();

class Particle {
  constructor() { this.reset(); }
  reset() {
    this.x = Math.random() * canvas.width;
    this.y = Math.random() * canvas.height;
    this.vx = (Math.random() - 0.5) * 0.3;
    this.vy = (Math.random() - 0.5) * 0.3;
    this.size = Math.random() * 1.5 + 0.3;
    this.alpha = Math.random() * 0.4 + 0.05;
    const colors = ['106,180,212','212,168,74','212,104,120','109,196,152','200,184,152','255,251,224'];
    this.color = colors[Math.floor(Math.random() * colors.length)];
    this.life = 0; this.maxLife = Math.random() * 300 + 150;
  }
  update() {
    this.x += this.vx; this.y += this.vy; this.life++;
    if (this.life > this.maxLife) this.reset();
  }
  draw() {
    const a = this.alpha * Math.sin(Math.PI * this.life / this.maxLife);
    ctx2.beginPath();
    ctx2.arc(this.x, this.y, this.size, 0, Math.PI * 2);
    ctx2.fillStyle = `rgba(${this.color},${a})`;
    ctx2.fill();
  }
}

for (let i = 0; i < 80; i++) particles.push(new Particle());

function animateParticles() {
  ctx2.clearRect(0, 0, canvas.width, canvas.height);
  particles.forEach(p => { p.update(); p.draw(); });
  requestAnimationFrame(animateParticles);
}
animateParticles();
</script>
</body>
</html>
